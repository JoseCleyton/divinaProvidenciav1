export class User {
    public login: string;
    public password: string;
    public name: string;
    public admin: boolean
    constructor(login: string, password: string){
        login = login;
        password= password
    }
}